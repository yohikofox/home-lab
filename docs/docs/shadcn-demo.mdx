---
sidebar_position: 999
title: "ShadcnUI Components Demo"
description: "Test des composants ShadcnUI de base"
---

import { Button } from '../src/components/ui/button';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from '../src/components/ui/card';
import { Alert, AlertTitle, AlertDescription } from '../src/components/ui/alert';
import { Badge } from '../src/components/ui/badge';

# ShadcnUI Components Demo

Cette page démontre les composants ShadcnUI de base, prêts pour une nouvelle conception UI.

## Boutons

### Variantes Standard
<div className="flex gap-4 mb-6 flex-wrap">
  <Button>Default Button</Button>
  <Button variant="secondary">Secondary</Button>
  <Button variant="outline">Outline</Button>
  <Button variant="ghost">Ghost</Button>
  <Button variant="link">Link</Button>
</div>

### Tailles
<div className="flex gap-4 mb-6 flex-wrap items-center">
  <Button size="sm">Small</Button>
  <Button size="default">Default</Button>
  <Button size="lg">Large</Button>
  <Button size="icon">🚀</Button>
</div>

## Cartes

### Carte Standard
<Card className="w-full max-w-md mb-6">
  <CardHeader>
    <CardTitle>Standard Card</CardTitle>
    <CardDescription>
      Une carte avec le style ShadcnUI par défaut
    </CardDescription>
  </CardHeader>
  <CardContent>
    <p>Contenu de la carte avec les styles de base.</p>
  </CardContent>
</Card>

### Cartes en Grid
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mt-8">
  <Card>
    <CardHeader>
      <CardTitle>Service 1</CardTitle>
      <CardDescription>Description du service</CardDescription>
    </CardHeader>
    <CardContent>
      <div className="text-sm space-y-1">
        <div>• Feature A</div>
        <div>• Feature B</div>
        <div>• Feature C</div>
      </div>
    </CardContent>
  </Card>

  <Card>
    <CardHeader>
      <CardTitle>Service 2</CardTitle>
      <CardDescription>Description du service</CardDescription>
    </CardHeader>
    <CardContent>
      <div className="text-sm space-y-1">
        <div>• Feature A</div>
        <div>• Feature B</div>
        <div>• Feature C</div>
      </div>
    </CardContent>
  </Card>

  <Card>
    <CardHeader>
      <CardTitle>Service 3</CardTitle>
      <CardDescription>Description du service</CardDescription>
    </CardHeader>
    <CardContent>
      <div className="text-sm space-y-1">
        <div>• Feature A</div>
        <div>• Feature B</div>
        <div>• Feature C</div>
      </div>
    </CardContent>
  </Card>
</div>

## Badges

<div className="flex gap-2 mb-6 flex-wrap">
  <Badge>Default</Badge>
  <Badge variant="secondary">Secondary</Badge>
  <Badge variant="outline">Outline</Badge>
  <Badge variant="destructive">Destructive</Badge>
</div>

## Alertes

<Alert className="mb-4">
  <AlertTitle>Information</AlertTitle>
  <AlertDescription>
    Ceci est une alerte d'information standard.
  </AlertDescription>
</Alert>

<Alert variant="destructive" className="mb-4">
  <AlertTitle>Erreur</AlertTitle>
  <AlertDescription>
    Ceci est une alerte d'erreur.
  </AlertDescription>
</Alert>

---

Base propre pour développer une nouvelle identité visuelle avec ShadcnUI.